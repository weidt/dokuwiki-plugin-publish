Plug-in to allow simple page approval process.

Added feature, to use tags from tag plugin: https://www.dokuwiki.org/plugin:tag
Configuration is done using webinterface from dokuwiki.

New Config Variables: 
apr_use_tag = Show pages only, if a separate tag has been set - disable for plugin behave as original intended
apr_tag = Show only pages having this tag